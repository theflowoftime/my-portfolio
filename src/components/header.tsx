import NavBar from "./sub-components/navbar";

function Header() {
  return (
    <header className="sticky top-0 z-20 ">
      {/* <header className=""> */}
      <NavBar />
    </header>
  );
}

export default Header;
